[package]
name = "lzodiff"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
lzo = "0.1"
# GPL-2.0 — used ONLY as a local differential oracle, never committed/distributed.
rustlzo = { package = "rust-lzo", version = "0.6" }

[[bin]]
name = "lzodiff"
path = "src/main.rs"
